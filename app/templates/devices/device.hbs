<div class="container">
  {{search-form action="search"}}
  <h2>Devices Found <span class="badge badge-success">{{devices.length}}</span></h2>
  <br>
  <div class="row">
    <div class="container view-buttons">
      <div class="col-xs-12">
          <button class="btn {{if brandIsHidden "btn-danger" "btn-success"}}" {{action 'toggleBrand'}}>Brand</button>
          <button class="btn {{if techIsHidden "btn-danger" "btn-success"}}" {{action 'toggleTech'}}>Techn</button>
          <button class="btn {{if gprsIsHidden "btn-danger" "btn-success"}}" {{action 'toggleGPRS'}}>GPRS</button>
          <button class="btn {{if edgeIsHidden "btn-danger" "btn-success"}}" {{action 'toggleEDGE'}}>EDGE</button>
          <button class="btn {{if cameraIsHidden "btn-danger" "btn-success"}}" {{action 'toggleCamera'}}>Camera</button>
      </div>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="container">
      {{#each devices as |device|}}
        <div class="col-sm-4">
          <div class="device panel panel-default">
            <div class="panel-heading">
              <h3 class="panel-title">{{device.DeviceName}}</h3>
            </div>
            <div class="panel-body">
              <p class="{{if brandIsHidden "hide" "show"}}">Brand : {{device.Brand}}</p>
              <p class="{{if techIsHidden "hide" "show"}}">Technology : {{device.technology}}</p>
              <p class="{{if gprsIsHidden "hide" "show"}}">GPRS : {{device.gprs}}</p>
              <p class="{{if edgeIsHidden "hide" "show"}}">Edge : {{device.edge}}</p>
              <p class="{{if cameraIsHidden "hide" "show"}}">Camera : {{device.camera}}</p>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>

</div>
{{outlet}}
